# 이 저장소에서 책을 쓸 때

자가출판 프로젝트입니다. 지금까지 여덟 권을 냈습니다.
한국어와 영어를 같이 내고, 성인판과 청소년판을 따로 만듭니다.

**새 책을 쓰기 시작하기 전에 이 파일을 끝까지 읽으세요.**

---

## 1. 문체 — 이게 제일 중요합니다

2026년 9월에 열두 권을 전부 재 봤더니 같은 자리에서 걸렸습니다.
`tools/voice.py` 가 그 검사기입니다.

제일 센 숫자 하나: 『다들 갖고 있었잖아』의 문장 1,772개 중
**60자를 넘는 문장이 0개**였습니다. 사람이 낼 수 있는 숫자가 아닙니다.

원인은 하나입니다.

> **모든 문장이 따로따로, 가장 또렷하게 읽히도록 다듬어졌습니다.**

문장 하나만 보면 전부 잘 쓴 문장이고, 그게 문제입니다.
사람이 쓴 글에는 값이 붙어 있습니다 — 길어진 자리, 일반화가 안 되는데 넣은 이야기,
안 풀고 넘어간 자리. 그 셋이 없으면 잘 쓴 글이 아니라 **사람이 없는 글**입니다.

### 지켜야 할 아홉

1. **한 쪽에 긴 문장 하나.** 20자에 안 들어가는 생각은 끊지 말고 그대로 씁니다.
   "쯤" "거의" "대체로" 같은 말을 지우지 마세요. 그게 사람입니다.
2. **굵은 글씨는 한 장에 다섯 번까지.** 지금까지는 장당 서른 번씩 썼습니다.
   지웠을 때 그 자리가 안 살아나면 문장이 약한 겁니다. 문장을 고치세요.
3. **장마다 생김새를 다르게.** 장면으로 여는 장, 질문으로 여는 장, 결론부터 던지는 장,
   앞 장에서 못 푼 것을 이어받는 장을 섞습니다. 「정리하면」 표는 **절반의 장에만** 붙입니다.
4. **장마다 하나는 안 풀고 끝냅니다.** "이건 나도 아직 모르겠어" 가 한 장에 하나씩.
5. **자기만 아는 구체를 한 장에 하나.** 아래 3번 항목에 방법을 적었습니다.
6. **'A가 아니라 B' 를 한 권에 스무 번 아래로.** 지금까지 권당 마흔다섯~쉰네 번 썼습니다.
7. **숫자로 안 떨어지는 목록을 만듭니다.** "셋이야" 가 계속 나오면 말버릇입니다.
   "크게 보면 둘이야. 세 번째가 있긴 한데 앞의 둘만큼 자주 있는 건 아니야."
8. **소리 내서 읽습니다.** 숨이 안 차면 1번으로, 지루하면 지웁니다.
9. **고칠 때 더하지 말고 뺍니다.** 고친 뒤 글자 수가 늘었으면 잘못 고친 겁니다.

### 검사

```
python3 tools/voice.py book-teen3     # 한 권
python3 tools/voice.py --all          # 전부
```

다섯 줄에 `OK` / `..` / `!!` 가 붙습니다. **`!!` 가 없어야 합니다.**
특히 **긴 문장 비율**과 **굵은 글씨** 둘은 반드시 잡으세요.

한 부를 끝낼 때마다 돌리세요. 다 쓰고 나서 고치려면 훨씬 비쌉니다.

자세한 진단과 고치기 전후 예시는 `사람이-쓴-글로-만드는-법.md` 에 있습니다.

---

## 2. 사실 — 확인 못 한 것은 통째로 뺍니다

이 프로젝트가 가장 일관되게 지켜 온 규칙입니다. 계속 지키세요.

- 확인은 **공개 자료 대조 수준**입니다. 1차 자료가 아닙니다. 그렇게 적으세요.
- 확인이 안 되면 **부드럽게 고치지 말고 잘라냅니다.**
  얼버무린 문장은 독자가 사실로 읽습니다.
- 각 장 끝의 「이 장의 출처」에 **확인한 것**과 **못 해서 뺀 것**을 둘 다 적습니다.
  둘째를 적는 책이 거의 없고, 그게 더 쓸모 있습니다.
- 실제 사례가 아닌 장면은 **장면이라고 밝힙니다.**
- 이름은 늙습니다. 회사·상품 이름은 꼭 필요할 때만 씁니다.
- 요즘·올해·최근을 쓰지 않습니다. 연도는 출처에만 씁니다.

### 발행을 막는 표시

원고에 이렇게 적으면 `--stat` 이 세고, 둘 다 0 이어야 발행할 수 있습니다.

```
<!-- 확인: 이 숫자를 등록 기록에서 직접 볼 것 -->
<!-- TODO: 여는 글에 넣을 가게 이름 -->
```

---

## 3. 작가의 구체를 받는 방법

**5번 규칙은 제가 대신 못 합니다. 지어내면 그게 제일 나쁜 거짓말이 됩니다.**

그래서 장을 쓸 때 **그 자리를 비우고 표시를 남깁니다.**

```
<!-- 확인: 겪은 일 — 이 자리에 작가가 직접 본 장면 한 개.
     지금 있는 '집마다 서랍이 하나씩 있어' 는 누구나 아는 장면이라
     아무 냄새가 안 납니다. 언제, 어디서, 누가를 넣어 주세요. -->
```

**장마다 하나씩 남깁니다.** 발행 전에 작가가 채웁니다.
채우기 전에는 `--stat` 이 0 이 안 되니 실수로 나갈 일이 없습니다.

기존 원고에서 바꿀 만한 자리의 예:

| 지금 | 바꿀 방향 |
|---|---|
| 집마다 케이블 모아 둔 서랍이 하나씩 있어 | 우리 집은 그게 전화기 놓는 탁자 맨 아래 칸이야 |
| 서점에 가면 두 자리가 있어 | 내가 몇 년 다닌 그 서점은 |
| 교실에서 다들 갖고 있던 게 있었어 | (작가가 실제로 본 것 하나) |

---

## 4. 작업 순서

지금까지는 이 순서였습니다.

```
기획을 받는다 → 내가 원고를 쓴다 → 같이 고친다
```

이러면 아무리 고쳐도 **초고를 AI가 쓴 책**이고, KDP 의 AI 문항 답은 `AI-generated` 입니다.
문체를 고쳐도 그 답은 안 바뀝니다. 그 칸은 만든 방법을 묻습니다.

**작가가 `AI-assisted` 로 답하고 싶다고 하면 순서를 이렇게 합니다.**

```
1. 작가가 장마다 겪은 일을 적는다        (짧아도 됨)
2. 작가가 초고를 쓴다                    (거칠어도 됨 — 이게 전부다)
3. 내가 구조를 점검한다                  (빠진 논리, 순서, 되풀이)
4. 내가 사실을 대조한다                  (2번 규칙 그대로)
5. 작가가 다시 쓴다                      (내 문장을 받아 적지 않는다)
6. 내가 voice.py 로 잰다
```

**5번에서 내 문장을 그대로 받아 적으면 위의 순서가 무의미해집니다.**
고칠 곳을 짚어 주되, 고친 문장을 통째로 내밀지 마세요.

작가가 그냥 써 달라고 하면 써 드리되, **AI 문항 답이 안 바뀐다는 것은 한 번 짚습니다.**
두 번 말하지는 마세요. 이미 아는 이야기입니다.

---

## 5. 만드는 도구

| 하는 일 | 명령 |
|---|---|
| 원고 → 웹 | `python3 tools/<책>.py` |
| 분량·확인·TODO | `python3 tools/<책>.py --stat` |
| 문체 검사 | `python3 tools/voice.py <책>` |
| 전자책 표지 | `python3 tools/cover.py <책>` |
| 인쇄용 감싸는 표지 | `python3 tools/wrap_cover.py <책> <쪽수>` |
| EPUB | `python3 tools/epub.py <책>` |
| PDF | `python3 tools/pdf.py <책> [--trim 6x9]` |
| 영어 문장 검사 | `python3 tools/en_lint.py <원고>/*.md` |
| 문서 → HTML | `python3 tools/doc2html.py <문서>.md` |

새 책을 시작하면 `tools/` 에 덮어쓰기 모듈을 하나 만듭니다.
`import book as m` 한 뒤 `m.TITLE`, `m.MANUSCRIPT` 같은 것을 바꾸는 방식입니다.
청소년판은 `import book_teen` 을 먼저 합니다. `tools/book_teen3.py` 가 최신 예입니다.

그리고 **`cover.py`, `epub.py`, `pdf.py` 의 목록에 새 책을 등록**해야 합니다.
셋 다 손대야 합니다. 하나라도 빠지면 그 단계에서 막힙니다.

`voice.py` 는 등록 안 해도 폴더 이름만 맞으면 돌아갑니다.
다만 `--all` 에는 안 잡히니 목록에 넣어 두는 편이 낫습니다.

---

## 6. 하지 말 것

**`wrap_cover.py` 를 인자 없이 부르지 마세요.** 예전에 `--all` 로 떨어져서
이미 낸 네 권의 표지를 말없이 다시 그린 적이 있습니다. 지금은 막아 뒀습니다.

**이미 낸 책의 파일을 다시 만들지 마세요.** 글꼴이나 컨테이너가 바뀌면
결과물이 미묘하게 달라집니다. 고칠 이유가 있을 때만 건드리고,
건드렸으면 `git status` 로 의도하지 않은 파일이 바뀌지 않았는지 봅니다.

**`발간본/` 은 발행 기록입니다.** 건드리지 마세요.

**커밋 메시지와 원고에 모델 이름을 적지 마세요.**

---

## 7. 문서를 낼 때

작가가 문서를 요청하면 거의 항상 **`(md/html)` 둘 다** 원합니다.

```
python3 tools/doc2html.py <문서>.md
```

KDP 등록용 문서는 `KDP-LISTING-BESTSELLER.md` 의 형식을 따릅니다.
설명문은 **HTML 로 주고, 에디터의 `Source` 버튼을 눌러 붙이라고** 적어야 합니다.
그냥 붙이면 태그가 글자로 보입니다.

---

## 8. 바깥으로 나가는 요청

**403 · 407 · EGRESS_BLOCKED 가 나오면 그 호스트는 허용되지 않은 것입니다.**
다시 시도하거나 우회하지 말고, 막힌 호스트를 그대로 알립니다.

`kdp.amazon.com` 은 막혀 있습니다. 그래서 KDP 의 규정을 직접 확인할 수 없습니다.
**확인 못 한 것은 확인 못 했다고 적으세요.** 지금 확인이 안 된 채로 남은 것 둘:

- 종이책 60% 구간의 `$9.99` 문턱
- Kindle Unlimited 쪽당 단가
